terraform {
    backend "s3" {
        bucket = "dinindunz-terralearn-tfstare"
        encrypt = true
        key = "terralearn/state.tfstate" # Where you want to store state in S3
        region = "us-east-1"
    }
}